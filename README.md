# a lil redirect server

## Development

```
npm install
npm run dev
```

The site should come up at `localhost:8080`. The following redirects currently exist:

- `/` --> allisonjking.com
- `/ppc` --> allisonjking.com/phoenix-pencil-company
